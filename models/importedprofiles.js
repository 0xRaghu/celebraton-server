const mongoose = require("mongoose");
const Schema = mongoose.Schema;

//Create Schema
const importedProfileSchema = new Schema({
  
});

module.exports = ImportedProfiles = mongoose.model("importedprofiles", importedProfileSchema);
